// examples/demo.ltl — runnable demo for ltl_fenwick

import std::io
use ltl_fenwick as lib

fn main() {
    io::println("ltl_fenwick demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}